SELECT 
    table_catalog AS database_name,
    table_schema,
    table_name,
    row_count,
    bytes / 1024 / 1024 AS size_mb
FROM 
    MYDB.information_schema.table_storage_metrics
ORDER BY 
    size_mb DESC;
