Sub InsertRowEveryThree()
    Dim i As Long
    Dim lastRow As Long
    lastRow = Cells(Rows.Count, "A").End(xlUp).Row
    For i = lastRow To 4 Step -3
        Rows(i).Insert Shift:=xlDown
    Next i
End Sub
